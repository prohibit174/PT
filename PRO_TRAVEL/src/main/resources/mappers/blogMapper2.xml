<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/td/mybatis-3-mapper.dtd">

<mapper namespace="kosta.travel.mappers.blogMapper2">

<insert id="insertBlog" parameterType="BlogVO">
		insert into blog(
		b_id, b_title, u_name, b_introduction, b_img
		)
		values(
		{b_id}, #{b_title}, #{u_name},#{b_introduction},#{b_img}
		)
		
	</insert>
</mapper>